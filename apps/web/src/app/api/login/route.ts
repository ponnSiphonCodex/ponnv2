import type { NextRequest } from "next/server";import { getCloudflareContext } from "@opennextjs/cloudflare";import { createDb } from "@/db";import { verifyPassword } from "@/lib/password";import { createSessionToken,buildSessionCookie } from "@/lib/session";import { notifyAdminChat } from "@/lib/notify";export const dynamic="force-dynamic";
export async function POST(req:NextRequest){const {env}=await getCloudflareContext({async:true});let b:any;try{b=await req.json()}catch{return Response.json({ok:false,error:"คำขอไม่ถูกต้อง"},{status:400})}const email=b.email?.trim(),pw=b.password;if(!email||!pw)return Response.json({ok:false,error:"กรอกอีเมลและรหัสผ่าน"},{status:400});if(!env.AUTH_SECRET||!env.NEXT_PUBLIC_SUPABASE_URL||!env.SUPABASE_SERVICE_ROLE_KEY)return Response.json({ok:false,error:"ระบบยังตั้งค่า Environment ไม่ครบ"},{status:500});try{const db=createDb(env),u=await db.prepare(`SELECT * FROM users WHERE email=?`).bind(email).first<any>();if(!u||!u.password_hash||!(await verifyPassword(pw,u.password_hash))){try{await db.prepare(`INSERT INTO login_logs(user_id,email,auth_provider,device_info,ip_address,success) VALUES(?,?,?,?,?,0)`).bind(u?.id??null,email,"Local",req.headers.get("user-agent")?.slice(0,180)??null,req.headers.get("cf-connecting-ip")??null).run()}catch{}return Response.json({ok:false,error:"อีเมลหรือรหัสผ่านไม่ถูกต้อง"},{status:401})}if(!u.active)return Response.json({ok:false,error:"บัญชีถูกปิดใช้งาน ติดต่อผู้ดูแล"},{status:403});await db.prepare(`INSERT INTO login_logs(user_id,email,auth_provider,device_info,ip_address,success) VALUES(?,?,?,?,?,1)`).bind(u.id,email,"Local",req.headers.get("user-agent")?.slice(0,180)??null,req.headers.get("cf-connecting-ip")??null).run();await db.prepare(`UPDATE users SET last_login_at=unixepoch() WHERE id=?`).bind(u.id).run();await notifyAdminChat(env,`🔑 <b>เข้าสู่ระบบ</b>\n${u.name??u.email} (Local)`);const token=await createSessionToken(env.AUTH_SECRET,{sub:u.id,email:u.email,name:u.name});const res=Response.json({ok:true});res.headers.append("Set-Cookie",buildSessionCookie(token));return res}catch(e){console.error("LOCAL_LOGIN_ERROR",e);return Response.json({ok:false,error:e instanceof Error?e.message:"เชื่อมต่อระบบไม่สำเร็จ"},{status:500})}}
