import {redirect} from "next/navigation";import {requireAuth} from "@/lib/page-auth";import {AppShell,PageHeader} from "@/components/app-shell";import {shellProps} from "@/lib/shell-props";import {ProjectWizard} from "@/components/project-wizard";
const d=(u:number|null)=>u?new Date(u*1000).toISOString().slice(0,10):"";
export default async function Page({params}:{params:Promise<{id:string}>}){const a=await requireAuth();if(!a)redirect("/login");if(!a.scope.isPmo)redirect("/pm/projects");const {id}=await params;const pid=Number(id);const [p,f,u,pr,pm,team,att]=await Promise.all([a.d1.prepare(`SELECT id,name FROM products ORDER BY name`).all(),a.d1.prepare(`SELECT id,name,product_id,project_id FROM features ORDER BY product_id,name`).all(),a.d1.prepare(`SELECT id,COALESCE(name,email) name FROM users WHERE active=1 ORDER BY name`).all(),a.d1.prepare(`SELECT * FROM projects WHERE id=?`).bind(pid).first<any>(),a.d1.prepare(`SELECT user_id FROM project_managers WHERE project_id=? LIMIT 1`).bind(pid).first<any>(),a.d1.prepare(`SELECT owner_user_id FROM team_roster WHERE project_id=? AND owner_user_id IS NOT NULL`).bind(pid).all(),a.d1.prepare(`SELECT file_name,gdrive_file_id,gdrive_web_link FROM attachments WHERE reference_type='project' AND reference_id=?`).bind(pid).all()]);if(!pr)redirect("/pm/projects");const initial={name:pr.name,description:pr.description,status:pr.status,productId:pr.product_id,startDate:d(pr.start_date),endDate:d(pr.end_date),pmId:pm?.user_id??"",featureIds:(f.results??[]).filter((x:any)=>x.project_id===pid).map((x:any)=>x.id),teamIds:(team.results??[]).map((x:any)=>x.owner_user_id),files:(att.results??[]).map((x:any)=>({fileId:x.gdrive_file_id,url:x.gdrive_web_link,fileName:x.file_name}))};return <AppShell active="project" {...shellProps(a)}><PageHeader title={`แก้ไข Project · ${pr.name}`}/><ProjectWizard products={p.results??[]} features={f.results??[]} users={u.results??[]} initial={initial} projectId={pid}/></AppShell>}
