import {redirect} from "next/navigation";import {requireAuth} from "@/lib/page-auth";import {AppShell,PageHeader} from "@/components/app-shell";import {shellProps} from "@/lib/shell-props";import {ProjectWizard} from "@/components/project-wizard";export default async function Page(){const a=await requireAuth();if(!a)redirect("/login");if(!a.scope.isPmo)redirect("/pm/projects");const [p,f,u]=await Promise.all([a.d1.prepare(`SELECT id,name FROM products ORDER BY name`).all(),a.d1.prepare(`SELECT id,name,product_id FROM features ORDER BY product_id,name`).all(),a.d1.prepare(`SELECT id,COALESCE(name,email) name FROM users WHERE active=1 ORDER BY name`).all()]);return <AppShell active="project" {...shellProps(a)}><PageHeader title="สร้าง Project ใหม่" subtitle="Product → Feature → Timeline → Project Manager → Working Team"/><ProjectWizard products={p.results??[]} features={f.results??[]} users={u.results??[]}/></AppShell>}